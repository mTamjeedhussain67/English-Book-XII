/**
 * HomepageFeatures is no longer used — the homepage has been fully redesigned.
 * This file is kept as an empty export to avoid import errors if referenced elsewhere.
 */
export default function HomepageFeatures() {
  return null;
}
